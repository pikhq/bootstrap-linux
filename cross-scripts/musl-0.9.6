url=http://www.etalabs.net/musl/releases/musl-$version.tar.gz

out="$top/cross"

config() {
	./configure --host=$A-unknown-linux-musl --prefix="$out"/$A-unknown-linux-musl/ --syslibdir="$out"/lib \
		--disable-gcc-wrapper --disable-shared
}

install() {
	make install
}
